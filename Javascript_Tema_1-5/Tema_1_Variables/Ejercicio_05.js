// Ahora utiliza el array que has creado anteriormente para recorrerlo y mostrar una a una todas las posiciones del array.

const info = ["36", "Canto", "Jose"];

for (let i = 0; i < info.length; ++i); 
// Iniciamos en 0 y vamos hasta la longitud del array, en este caso 3, por lo tanto el bucle se repetirá 3 veces, incrementando el valor de "i" de 1 en 1, en cada iteración.

console.log(info[0])
console.log(info[1])
console.log(info[2])