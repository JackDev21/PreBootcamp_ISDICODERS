// Añade la resta... 

// output: The sum and difference of 7 and 3 is 10 and 4.

const a = 7
const b = 3

const sum = a + b
const rest = a - b

console.log(`The sum and diferrence of ${a} + ${b} is ${sum} and ${rest}`)
