// Primero, creamos una función que nos cree un saludo, pasa tu nombre como argumento y devuélvelo por la consola.


// Este código define una función llamada saludo que toma un parámetro llamado name.
const saludo = (name) => {
 // La función devuelve el valor de name.
    console.log(`Mi nombre es ${name}`);
}

// Se define una variable name y se le asigna el valor "Jack".
const name = "Jack";

saludo(name)